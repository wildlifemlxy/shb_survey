{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for WWF Survey Telegram Bot",
  "main": "server.js",
"scripts": {
  "start": "pm2 start server.js",
  "dev": "pm2 start server.js --name dev-app --watch",
  "stop": "pm2 stop all",
  "list": "pm2 list",
  "logs": "pm2 logs",
  "restart": "pm2 restart all"
},
  "dependencies": {
    "axios": "^1.8.4",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "forever": "^4.0.3",
    "fs": "^0.0.1-security",
    "node-cron": "^3.0.3",
    "path": "^0.12.7",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
